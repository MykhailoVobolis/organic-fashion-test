export const selectLogo = (state) => state.content.logo;

export const selectNavItems = (state) => state.content.navItems;

export const selectHero = (state) => state.content.hero;

export const selectShop = (state) => state.content.shop;

export const selectCollection = (state) => state.content.collection;

export const selectAbout = (state) => state.content.about;

export const selectFollow = (state) => state.content.follow;
